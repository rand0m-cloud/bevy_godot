[package]
name = "scene_tree_test"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
path = "../run_godot.rs"
name = "run_scene_tree_test"

[lib]
crate-type = ["cdylib"]

[dependencies]
gdnative = "0.10"
bevy = {version = "0.8", default-features = false}
bevy_godot = {path = "../.."}