[package]
name = "spawn_scene"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
path = "../run_godot.rs"
name = "run_spawn_scene"

[lib]
crate-type = ["cdylib"]

[dependencies]
gdnative = "0.10"
bevy_godot = {path = "../.."}
bevy = {version = "0.8", default-features = false}